# Personas integration workspace

The integration launcher copies this fixture to a disposable directory before opening it in the
Extension Development Host. The unpacked extension below is intentionally only a candidate; the
test verifies that file presence is not mislabeled as installation.
